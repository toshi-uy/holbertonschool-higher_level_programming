#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    if my_list:
        if idx < 0:
            return my_list1
        elif idx > len(my_list) - 1:
            return my_list1
        else:
            my_list.copy()
            my_list[idx] = element
            return my_list
